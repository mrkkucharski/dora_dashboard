{
  "name": "dora-kpi-dashboard",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "preview": "npm --prefix app run preview",
    "refresh:data": "python3 scripts/normalize_events.py && python3 scripts/compute_dora.py"
  }
}
